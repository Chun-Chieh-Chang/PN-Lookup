# PORT: Express 伺服器監聽埠（預設 3001，vite dev 已 proxy /api → localhost:3001）
PORT="3001"

# VITE_STATIC_ONLY: 設為 "true" 時啟用 GitHub Pages 靜態模式（關閉 API 呼叫）
# 由 .github/workflows/deploy.yml 在建置時注入
VITE_STATIC_ONLY="false"

# DISABLE_HMR: 設為 "true" 時停用 Vite HMR（供 CI / 沙盒環境使用）
DISABLE_HMR="false"
